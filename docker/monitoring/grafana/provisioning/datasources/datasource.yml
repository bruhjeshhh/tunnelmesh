apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus is at localhost:9090 with /prometheus/ path prefix
    url: http://localhost:9090/prometheus
    isDefault: true
    editable: false
